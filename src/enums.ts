export enum TemplateEnum {
    push = "push",
    issues = "issues",
    pull_request = "pull_request",
    pull_request_target = "pull_request_target",
    schedule = "schedule",
    default = "default"
}